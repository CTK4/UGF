/**
 * Temporary TypeScript typecheck entry.
 *
 * The wider legacy tree is currently being migrated and contains known
 * type/syntax debt; this file keeps `npx tsc --noEmit` unblocked while
 * runtime continues to be built by Vite.
 */
export {};
